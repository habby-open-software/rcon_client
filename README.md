rcon.php will return true or false, supported single & multiple commands in one connection<br>
Make rcon_host & rcon_port is defined and then call SendRcon with the data array of supported arcturus RCON commands
